fixtures:
  repositories:
    stdlib:
      repo: "git://github.com/puppetlabs/puppetlabs-stdlib"
      ref: "3.2.1"
    apt:
      repo: "git://github.com/puppetlabs/puppetlabs-apt"
      ref: "1.5.2"
  symlinks:
    omd: "#{source_dir}"
